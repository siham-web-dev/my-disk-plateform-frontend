import DriveContainer from "@/components/custom/containers/DriveContainer";

const page = () => {
  return <DriveContainer needsToToggle={true} currentFolder="Shared with me" />;
};

export default page;
